name: urbanvoice
description: UrbanVoice - Aplicación de seguridad ciudadana
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.4.0
  flutter_bloc: ^8.1.3
  get_it: ^7.6.4
  go_router: ^13.0.0
  google_maps_flutter: ^2.5.3
  google_maps_flutter_web: ^0.5.4+3
  equatable: ^2.0.5
  dartz: ^0.10.1
  geolocator: ^10.1.0
  image_picker: ^1.0.7
  intl: ^0.19.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
